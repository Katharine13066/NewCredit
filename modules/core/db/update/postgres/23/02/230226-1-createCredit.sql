create table CREDIT_CREDIT (
    CARD_ID uuid,
    --
    NUMBER_ varchar(50),
    CREDIT_TYPE_ID uuid,
    BANK_ID uuid,
    DATE_ date,
    AMOUNT double precision,
    --
    primary key (CARD_ID)
);